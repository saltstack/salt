Documented the grains that fail or return empty when the
salt-master runs as a non-root user (``mdadm``, ``smartos``,
``disks``, ``iscsi``, ``opts.fips_enabled``) under the
"Grains when the master runs as non-root" section of
``doc/ref/configuration/nonroot.rst``.
