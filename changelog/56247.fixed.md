Added an end-to-end aws_kms pillar walkthrough at
``doc/topics/tutorials/aws_kms_pillar.rst`` covering KMS CMK
creation, wrapping the data key, encrypting a value, decrypting on
the master, common failure modes and key rotation. The renderer
docstring now links to the walkthrough and the YAML example uses
``!!binary |`` so the data key parses correctly.
