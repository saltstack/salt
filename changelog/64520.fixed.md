Restore the `opennebula` cloud driver and fix the `create()` docstring to remove the misleading `salt-cloud -p my-opennebula-profile vm_name memory=16384 cpu=2.5 vcpu=16` example, which created four separate VMs instead of overriding profile values.
