Move `per_remote_overrides`, `per_remote_only`, and `global_only` defaults into `GitFS`, `GitPillar`, and `WinRepo` classes in `salt.utils.gitfs`, eliminating the need for callers to pass these arguments explicitly. Passing these arguments is now deprecated and will be removed in Potassium.
