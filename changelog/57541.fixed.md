``network.arp`` now falls back to ``ip neigh`` (iproute2) on Linux when the deprecated net-tools ``arp`` command is not installed, instead of raising ``CommandNotFoundError``. Completes the net-tools migration started in #64351; net-tools is deprecated and absent by default on modern distributions.
