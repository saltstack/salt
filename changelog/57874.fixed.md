Replaced the publisher_acl and external_auth examples with quoted
glob targets that parse cleanly, and added an explicit warning that
function names are matched as regular expressions rather than shell
globs (so ``pkg.*`` matches ``pkgin`` and ``pkg_resource`` as well
as the ``pkg`` module). The new
``tests/pytests/unit/test_acl_examples.py`` pins each documented
example through ``CkMinions.auth_check`` / ``spec_check``.
