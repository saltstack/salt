Replace deprecated ``datetime.utcnow()`` and ``datetime.datetime.utcnow()`` with timezone-aware ``datetime.now(tz=datetime.timezone.utc)`` across the codebase to clear the Python 3.12+ ``DeprecationWarning``.
