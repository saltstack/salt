An invalid _schedule.conf configuration file is renamed to _schedule.confYAMLError.
This avoids disabling the minion and busy polling the CPU.
Other invalid configuration files will delay an exception.
This only avoids busy polling the CPU.