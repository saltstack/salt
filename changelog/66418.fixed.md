Documented that the `pip` execution module and `pip.installed` state default to Salt's own bundled relenv pip (not the system Python) when `bin_env` is omitted on a onedir install, and how to pass `bin_env` to target the system Python instead.
