Fixed SyncClientMixin.low() raising a misleading UnboundLocalError for proc_fn when a runner/wheel call failed before its job tracker file was written, which hid the real error and skipped the job cache and return event.
