Fixed the ``win_task`` execution module so that ``repeat_duration="Indefinitely"`` actually produces an indefinite repetition pattern. The internal duration table now maps ``Indefinitely`` to the documented ISO 8601 value ``PT0S`` instead of an empty string, which the Windows Task Scheduler treated as "no duration set" and silently disabled repetition.
