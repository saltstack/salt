Fixed salt-ssh's scp-based file transfer hanging indefinitely on targets using OpenSSH's legacy scp protocol (e.g. Photon OS 4) when the destination path contains a space. Transfers now go through a space-free staging path and are renamed into place over a plain ssh command, avoiding scp's own internal remote-path re-quoting.
