Fixed salt-ssh silently discarding a failed thin/ext_mods tarball transfer and recursing into an unbounded deploy-retry loop instead of returning an error. The failed transfer's error is now surfaced immediately, and a second consecutive deploy failure returns an error rather than retrying again. What previously looked like salt-ssh hanging for over 90 seconds was actually dozens of failed deploy/shim cycles.
