Documented the ESXi proxy minion's vCenter authentication options (``vcenter``, ``esxi_host``, ``mechanism``, ``domain``, ``principal``) in ``salt.proxy.esxi`` and the ESXi proxy minion tutorial. The tutorial now shows worked pillar examples for the direct-ESXi, ``mechanism: userpass`` vCenter, and ``mechanism: sspi`` vCenter connection modes.
