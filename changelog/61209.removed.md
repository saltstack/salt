Removed the orphaned `salt.utils.namecheap` module. It was only used by the `namecheap_*` execution and state modules, which were moved out to the `saltext.namecheap` community extension, leaving it with no remaining consumers in Salt.
