Fix `boto_sns.__init__()` such that we can run test cases.  Fix support for returning more than 100 topics in `boto_sns.get_all_topics()`. Do not fail in `boto_sns.delete()` if topic is already deleted (does not exist). Fix support for returning more than topics from `boto_sns.get_all_subscriptions()`. Fix `boto_sns.subscribe()` such that it returns false on failure to subscribe. Return a failure on `boto_sns.unsubscribe()` if the specified topic does not exist. Return success on `boto_sns.unsubscribe()` if unready unsubscribed.
