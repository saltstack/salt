Added a "Key signing in HA topologies" section to the High Availability docs explaining the two supported key topologies (shared ``master.pem``/``master.pub`` vs. shared signing key pair ``master_sign.*``) and clarified in the Multi-Master-PKI tutorial that the signing key pair must be identical on every master in the HA pool while the per-master key pair remains unique.
