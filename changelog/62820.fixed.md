Documented the onedir packaging implications for dependency CVEs
in the new ``doc/security/threat-model.rst`` page: distro
``python3-*`` security updates do not patch the Salt onedir
because the onedir ships its own Python and its own dependency
copies; the supported fix path is the next Salt release.
