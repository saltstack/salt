Documented in the new ``doc/security/key-lifecycle.rst`` that
changing :conf_minion:`id` (or letting the FQDN-derived default
change) makes the minion present a new ``(id, pubkey)`` tuple to
the master and orphans the old accepted key. Pin
:conf_minion:`id` to an explicit string in any environment where
hostname or DNS is not stable.
