Documented ``@wheel``, ``@runner``, ``@jobs`` and the per-module
``@<modname>`` / function-list forms for ``publisher_acl`` and
added a unit-test in ``tests/pytests/unit/test_acl_examples.py``
that authorizes through ``CkMinions.wheel_check`` and
``runner_check`` so the documented syntax is pinned to the parser.
