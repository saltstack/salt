Removed the orphaned `salt.utils.kickstart`, `salt.utils.preseed` and `salt.utils.yast` modules. These were only ever used by the `genesis` execution module, which was moved out to a community extension, leaving them with no remaining consumers in Salt.
