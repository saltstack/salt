Updated the ``saltutil.regen_keys`` docstring to call out that it
only deletes the existing minion key files; new keys are generated
the next time the ``salt-minion`` process starts, so a restart is
required after the call. Cross-linked to the new key-lifecycle
reference page.
