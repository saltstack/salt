Clarified :conf_minion:`rejected_retry` to call out that it covers
both rejected keys (``salt-key -r``) and denied keys (master
auto-deny on ID collision); the master returns the same
``Reject: rejected master public key`` to the minion in either
case.
