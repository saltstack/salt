Fix `pkg.list_upgrades` on dnf/dnf5 to omit versionlocked / held packages by invoking `dnf check-update` instead of `dnf list --upgrades`, and strip the trailing `Obsoleting Packages` section so obsoleted (installed) packages are no longer reported as available upgrades.
