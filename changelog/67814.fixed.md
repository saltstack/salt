Fixed ``redis_cache`` plugin: ``_build_bank_hier()`` now records child bank
names in parent bank sets instead of the sentinel dot ``"."``, and ``list_()``
now queries the bank-hierarchy set first so that ``manage.*`` runners retrieve
the correct list of minion IDs rather than an empty list.
