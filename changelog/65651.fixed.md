Fix ``file.manage_file`` race condition where a copied file temporarily had 0600 permissions and the running process's ownership. The destination file now has the requested mode and owner applied atomically as part of the copy. Fixes ownership regression when only mode is specified and preserves prior file ownership when no user/group arguments are given.
