Added a ``session_store: cache`` option to the ``rest_cherrypy`` netapi. When set, CherryPy sessions are stored in the master's configured cache instead of per-worker memory, so salt-api sessions are shared across multiple salt-api workers and master-cluster members. This avoids spurious ``401`` responses when a client's requests are spread across more than one worker (for example behind a load balancer).
