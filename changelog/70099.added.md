Enabled the nightly stress test on the `3006.x` branch by renaming its `nightly-stress-test.yml` workflow to `run-stress-test.yml` and adding a required `branch` input, matching the pattern already used on `master`. Also added a byte-identical `nightly-stress-test.yml` dispatcher so `workflow_dispatch --ref 3006.x` calls from `master`'s dispatcher can find and trigger it.
