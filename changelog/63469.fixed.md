Clarified that AES key rotation on the master is triggered by both
``salt-key -d`` (delete) **and** ``salt-key -r`` (reject) — the
``intro_scale`` tutorial only mentioned deletion. Cross-linked to
the new key-lifecycle reference page.
