Updated the "Getting Started with VMware" cloud documentation: replaced the broken vSphere ``GuestOsIdentifier`` link, clarified which guest customization operations require ``customization: True`` (hostname, static IP/gateway/subnet/DNS, domain join, and ``win_run_once``), added a worked ``win_run_once`` example, and documented the previously undocumented ``customization_spec`` profile key.
