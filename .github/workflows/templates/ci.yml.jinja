<%- extends 'layout.yml.jinja' %>
<%- set pre_commit_version = "3.0.4" %>

<%- block on %>
on:
  push: {}
  pull_request: {}
<%- endblock on %>


<%- block jobs %>
  <{- super() }>

  job-1:
    name: Job 1
    runs-on:
      - self-hosted
      - macos
      - x86_64
    steps:

      - name: Cat File
        run: |
          cat ~/foo.txt || echo "File does not exist"

      - name: Set File
        run: |
          echo FOO >> ~/foo.txt

      - name: Cat File
        run: |
          cat ~/foo.txt || echo "File does not exist"

  job-2:
    name: Job 2
    runs-on:
      - self-hosted
      - macos
      - x86_64
    steps:

      - name: Cat File
        run: |
          cat ~/foo.txt || echo "File does not exist"

<%- endblock jobs %>
