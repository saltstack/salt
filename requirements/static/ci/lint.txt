# Lint requirements

docker >= 7.1.0; python_version >= '3.8'
docker < 7.1.0; python_version < '3.8'
# pylint 4 requires Python>=3.10; 3007.x still builds py3.9 onedirs
# so use the 3.x line on 3.9 and the 4.x line everywhere else.
pylint~=3.1.0; python_version < '3.10'
pylint~=4.0.5; python_version >= '3.10'
SaltPyLint>=2024.2.5
toml
