diff a/salt/minion.py b/salt/minion.py	(rejected hunks)
@@ -2529,6 +2539,8 @@ class Minion(MinionBase):
         This method should be used as a threading target, start the actual
         minion side execution.
         """
+        if "jid" in data:
+            log.warning("[PERF_TRACE] _thread_return start jid=%s", data["jid"])
         loop = asyncio.new_event_loop()
         asyncio.set_event_loop(loop)
